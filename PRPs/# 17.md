I need to upgrade the **Jukebox Mode** with a "Power User" data grid.

Please read `src/publishing/template/index.html.j2` and modify the HTML/CSS/JS to implement the following:

**1. Add "Top Toolbar" (Above the Table):**
   - Create a container `#table-toolbar` above the `<table>`.
   - **Left:** Add a Search Input (`<input type="text" id="table-search" placeholder="Search library...">`).
   - **Right:** Add a "Columns" button (`<button id="btn-cols">👁 Columns</button>`) that will eventually open a visibility modal.

**2. Update Table Headers (`<thead>`):**
   - Replace the current headers with this exact list (add distinct class names for styling):
     1. `#` (Index)
     2. `Watched` (Checkbox)
     3. `Thumbnail` (Img)
     4. `Title`
     5. `TL;DR` (Text)
     6. `Orig. Date` (Text)
     7. `Upload Date` (Text)
     8. `Channel` (Text)
     9. `Desc` (Text)
     10. `Tags` (Pills)
     11. `Dur.` (Text)
     12. `Rating` (Stars)
     13. `Notes` (Icon)
   - **Important:** Add a CSS class `.resizable-col` to all headers.
   - **Visuals:** Add CSS to ensure clear **Vertical Lines** between all columns (`border-right: 1px solid #233648;`).

**3. Update `renderTable()` Function:**
   - Update the row generation loop to match these new columns.
   - **Data Handling:** Since some fields (TL;DR, Original Date, Rating) don't exist in the data yet, please display placeholders (e.g., "-" or "N/A") or generate dummy data for now so the table doesn't crash.
   - **Specific Renderers:**
     - **Watched:** Render a checkbox `<input type="checkbox" onchange="toggleWatched(index)">`.
     - **Thumbnail:** Render `<img src="${video.thumbnail_url}" style="height: 30px;">`.
     - **Rating:** Render 5 star characters (☆☆☆☆☆).
     - **Tags:** Render simple `<span>` badges.
   - **Retain Logic:** Ensure `onclick="handleRowClick"` and `ondblclick="loadVideoByIndex"` are still applied to the `<tr>`.

**4. CSS Updates:**
   - Add styling for the vertical lines.
   - Add styling for `.star-rating` (color gold).
   - Ensure the table scrolls horizontally if it gets too wide (`overflow-x: auto` on the container).

Please implement this new grid structure.
We have two regressions that need immediate fixing in `src/publishing/template/index.html.j2`:

**1. Fix Column Resizing (The "3-to-1" Lag):**
The current resize logic feels "slow" or disconnected from the mouse. We need **1-to-1 pixel precision**.
- **Update `makeColumnsResizable`:** - On `mousedown`, capture `startX = e.clientX` and `startWidth = th.offsetWidth`.
  - On `mousemove`, calculate `delta = e.clientX - startX`.
  - Set `newWidth = startWidth + delta`.
  - Apply `th.style.width = newWidth + 'px'`.
  - *Crucial:* Ensure the table has `table-layout: fixed;` in CSS so columns respect exact pixel widths.

**2. Restore Sorting:**
Sorting broke when we updated the headers. 
- **Update `renderTable`:** Ensure that the generated `<th>` elements (except for index/thumbnail/notes) have the `onclick="sortPlaylist('key')"` attribute re-attached.
- **Visuals:** Add the sort indicators (▲/▼) back next to the text.

**Please REPLACE the `makeColumnsResizable` function and the header generation part of `renderTable` with this corrected logic:**

```javascript
// --- 1. PRECISE RESIZING LOGIC ---
function makeColumnsResizable() {
    const table = document.querySelector('table');
    // Ensure table respects manual widths
    table.style.tableLayout = 'fixed'; 
    
    document.querySelectorAll('.col-resizer').forEach(resizer => {
        resizer.addEventListener('mousedown', function(e) {
            e.preventDefault();
            const th = resizer.parentElement;
            const startX = e.clientX;
            const startWidth = th.offsetWidth;

            function onMouseMove(moveEvent) {
                // Exact 1:1 pixel movement
                const delta = moveEvent.clientX - startX;
                const newWidth = startWidth + delta;
                
                // Safety minimum of 30px
                if (newWidth > 30) {
                    th.style.width = newWidth + 'px';
                    // Force min-width to match to prevent flex shrinking
                    th.style.minWidth = newWidth + 'px'; 
                }
            }

            function onMouseUp() {
                window.removeEventListener('mousemove', onMouseMove);
                window.removeEventListener('mouseup', onMouseUp);
            }

            window.addEventListener('mousemove', onMouseMove);
            window.addEventListener('mouseup', onMouseUp);
        });
    });
}

// --- 2. RESTORE SORTING IN HEADERS ---
// (Snippet for renderTable header loop)
// When generating <th>:
/*
   const isSortable = !['index', 'thumbnail', 'notes'].includes(col.key);
   const clickAttr = isSortable ? `onclick="sortPlaylist('${col.key}')"` : '';
   const cursorClass = isSortable ? 'cursor-pointer' : '';
   
   // Render:
   <th class="${col.class} ${cursorClass}" ${clickAttr}>
       ${col.label} ${getSortIcon(col.key)}
       ${isResizable ? '<div class="col-resizer"></div>' : ''}
   </th>
*/
We are on the final step: **Step 4: Resizable Layouts**.

Please read `src/publishing/template/index.html.j2` and add draggable splitters to the UI.

**Requirements:**

1.  **HTML Structure:**
    * Add a vertical splitter `div` (id="splitter-vertical") between `#left-panel` and `#player-panel`.
    * Add a horizontal splitter `div` (id="splitter-horizontal") between `#video-stage` and the controls below it (only visible in Study Mode if needed, or adjust the Player Panel height in Jukebox mode).
    * *Note:* The main priority is resizing the **Sidebar Width** in Study Mode and the **Player Height** in Jukebox Mode.

2.  **CSS:**
    * Style the splitters: `width: 4px` (vertical) or `height: 4px` (horizontal), background `#233648`, cursor `col-resize` or `row-resize`.
    * Add a `:hover` state with `background: #137fec` so the user knows they can grab it.

3.  **JavaScript Logic:**
    * Create a `makeResizable()` function that initializes the drag logic.
    * **Vertical Resize (Study Mode):** When dragging the vertical splitter, update the `width` of `#left-panel`.
    * **Horizontal Resize (Jukebox Mode):** When dragging the horizontal splitter (top of player panel), update the `height` of `#player-panel`.
    * **Persistence:** Save the preferred width/height to `localStorage` (e.g., `layout_sidebar_width`) so the layout stays the same when I refresh.

**Please implement these layout changes in the template file.**
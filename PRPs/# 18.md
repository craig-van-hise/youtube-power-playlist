The Grid visuals are ready. Now I need to activate the **Table Logic**.

Please update `src/publishing/template/index.html.j2` with the following features:

**1. Search Functionality:**
   - Add an event listener to `#table-search` (on `input`).
   - Filter the `playlist` array based on the text (match Title, Channel, or Tags).
   - Call `renderTable()` with the filtered list.

**2. Column Visibility Modal:**
   - **State:** Add a `visibleColumns` object (e.g., `{ index: true, watched: true, tldr: false, ... }`).
   - **Modal:** Create a simple HTML Modal (`<dialog id="col-modal">`) containing checkboxes for each column.
   - **Button:** Wire `#btn-cols` to open the modal (`document.getElementById('col-modal').showModal()`).
   - **Logic:** When a checkbox changes, update `visibleColumns` and re-run `renderTable()`.
   - **Update Render:** Modify `renderTable` to only generate `<th>` and `<td>` elements if their column key is `true` in `visibleColumns`.

**3. Column Resizing (Click & Drag):**
   - **Add Grips:** In your table header generation, inject a `<div class="col-resizer"></div>` inside each `<th>`.
   - **CSS:** Style `.col-resizer` as a thin clickable area on the right edge of the header (`width: 5px; cursor: col-resize; position: absolute; right: 0;`).
   - **Logic:** Add a `makeColumnsResizable()` function.
     - On `mousedown` of a resizer, track the starting X and starting Width of the `<th>`.
     - On `mousemove` (window), update the `<th>` width.
     - On `mouseup`, stop dragging.

**Important:** Ensure the "Search" filters the *currently sorted* list, so sorting and searching work together.
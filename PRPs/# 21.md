
### 🎚️ The Transport Scrubber 

We are ready to add the **Transport Scrubber** (Timeline) to the Player Panel.

Please update `src/publishing/template/index.html.j2` to implement the video timeline controls.

**1. HTML Update (Add to `#player-controls`):**
   - Insert this specific HTML block at the top of the `#player-controls` div (above the "Previous/Next" buttons).
   
   ```html
   <div id="transport-bar" style="display: flex; align-items: center; gap: 12px; padding: 10px 15px; background: #0d1218; border-bottom: 1px solid #233648;">
       <button id="btn-transport-play" onclick="togglePlay()" style="background:none; border:none; color:white; cursor:pointer;">▶</button>
       
       <span id="time-current" style="font-family: monospace; font-size: 12px; color: #8b9bb4; min-width: 40px; text-align: right;">0:00</span>
       
       <input type="range" id="scrubber" min="0" value="0" step="1" style="flex: 1; cursor: pointer; accent-color: #137fec;">
       
       <span id="time-total" style="font-family: monospace; font-size: 12px; color: #8b9bb4; min-width: 40px;">0:00</span>
   </div>

```

**2. JavaScript Logic:**

* **State Tracking:** Create a variable `let isDraggingScrubber = false;`.
* **Update Loop:** Add a `setInterval` (every 500ms) that does the following:
* Check if `player` exists and `player.getCurrentTime` is available.
* If `!isDraggingScrubber`, update the `#scrubber` value and the `#time-current` text to match the video.
* Update `#time-total` with `player.getDuration()`.


* **Interaction Events:**
* `scrubber.oninput`: Set `isDraggingScrubber = true`. Update `#time-current` visually so numbers change while dragging.
* `scrubber.onchange`: Set `isDraggingScrubber = false`. Call `player.seekTo(this.value, true)` to jump the video.



**3. formatting Helper:**

* Add a helper function `formatTime(seconds)` to convert `125` into `"2:05"` for the displays.

Please integrate this timeline functionality.

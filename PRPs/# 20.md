We need to refine the Table Logic before moving on.

Please update `src/publishing/template/index.html.j2` with these specific tweaks:

**1. Disable Resizing for Static Columns:**
   - Modify the header generation logic in `renderTable`.
   - **Do NOT** add the `<div class="col-resizer">` element if the column is one of these:
     `['index', 'watched', 'thumbnail', 'orig_date', 'upload_date', 'rating', 'duration']`
   - Only dynamic columns (Title, TL;DR, Channel, Desc, Tags, Notes) should be resizable.

**2. Optimize Column Widths (CSS):**
   - Add these specific width rules to the `<style>` block to ensure the static columns fit their data perfectly:
     ```css
     /* Fixed Width Columns */
     .col-index { width: 40px; text-align: center; }
     .col-watched { width: 40px; text-align: center; }
     .col-thumbnail { width: 120px; } /* Fits a small thumbnail */
     .col-orig_date, .col-upload_date { width: 110px; font-family: monospace; }
     .col-rating { width: 100px; }
     .col-duration { width: 70px; text-align: right; font-family: monospace; }
     
     /* Dynamic Columns (Allow to grow) */
     .col-title { min-width: 200px; }
     .col-channel { min-width: 150px; }
     ```

**3. Update Visibility Modal:**
   - Inside the `showColumnModal()` function (or wherever you generate the modal HTML), add a **"Select All"** checkbox at the very top.
   - **Logic:**
     - When "Select All" is checked -> Check all column boxes -> Update `visibleColumns` to all true.
     - When "Select All" is unchecked -> Uncheck all boxes -> Update `visibleColumns` to all false.
     - Call `renderTable()` immediately after toggling.

Please apply these refinements to the grid.
Now for the final Jukebox feature: **The Transport Scrubber**.

Please update `src/publishing/template/index.html.j2` to add a video timeline slider.

**1. HTML Update:**
   - Inside `#player-controls`, add:
     ```html
     <div id="transport-container" style="display: flex; align-items: center; gap: 10px; padding: 10px;">
         <span id="time-current">0:00</span>
         <input type="range" id="scrubber" min="0" value="0" style="flex: 1;">
         <span id="time-total">0:00</span>
     </div>
     ```

**2. JavaScript Logic:**
   - **Update Interval:** Create a `setInterval` (running every 500ms) that checks `player.getCurrentTime()` and `player.getDuration()`.
   - **Sync Slider:** Update the `#scrubber` value and the time labels (`span`) to match the video progress.
   - **User Interaction:** Add an `input` listener to `#scrubber`. When the user drags it, call `player.seekTo(value, true)` to jump to that timestamp.
   - **Safety:** Ensure this logic only runs if `player` is valid and the state is PLAYING or PAUSED.

Please integrate this timeline control.